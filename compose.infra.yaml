services:
  postgres:
    image: postgres
    restart: always
    volumes:
      - postgres-data:/var/lib/postgresql
    networks:
      - backend
    env_file: ".env"

  redis:
    image: redis
    restart: always
    volumes:
      - redis-data:/data
    networks:
      - backend
